# Client

## eslint with vscode
In order to enable eslint features in vscode (or other editor) the client's dev dependencies must be installed on the host (not just in the docker container.

```
# Install npm with dev dependencies
npm install -D
```
